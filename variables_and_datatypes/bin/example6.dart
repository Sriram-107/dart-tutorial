void main() {
  const int age1 = 10;
  const int age2 = 20; // Dart infers datatype automatically.
  // No difference between the above 2. If working in a project if our team specify the datatype we can specify.
}
