void main() {
  final double age =
      20; // We can assign an int value(less precise) to double variable(more precise)
  // final int age2 = 20.2; // We cannot assign double to int.
  print(age);
}
