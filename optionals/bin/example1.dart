void main(List<String> args) {
  // Optional - Either a variable contains a value or not contains a value.
  const String? name =
      null; // Optional String - It can either contain a value or not contain a value.
  print(name);
  print(null);
}
